spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${MYSQL_URL}
    username: ${MYSQL_USERNAME:admin}
    password: ${MYSQL_PASSWORD:adgjl08642}
  redis:
    database: 1
    host: ${REDIS_HOST:124.221.250.220}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:adgjl08642}
  cloud:
    nacos:
      username: ${NACOS_USERNAME:nacos}
      password: ${NACOS_PASSWORD:adgjl08642}
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:124.221.250.220:8848}
        group: ${NACOS_DISCOVERY_GROUP:DEFAULT_GROUP}

file:
  minio:
    endpoint: ${MINIO_ENDPOINT:http://124.221.250.220:9002}
    publicEndpoint: ${MINIO_PUBLIC_ENDPOINT:http://124.221.250.220:9002}
    accessKey: ${MINIO_ACCESS_KEY:admin}
    secretKey: ${MINIO_SECRET_KEY:adgjl08642}
    bucket: ${MINIO_BUCKET:myoj}

codesandbox:
  url: ${CODESANDBOX_URL:http://127.0.0.1:8090/executeCode}
  secretKey: ${CODESANDBOX_SECRET_KEY:adgjl08642}
